# DevDocs AI — Frontend

Next.js dashboard and docs preview UI. Consumes the backend API.

## Run

```bash
npm run dev
```

## API

Set `NEXT_PUBLIC_API_URL` (default: `http://localhost:4000`) to point at the backend.
